import { FloatingNav } from "@/components/nav/FloatingNav";

/** Navigation chrome: a single movable bubble menu, rendered above every page. */
export function SiteHeader() {
  return <FloatingNav />;
}
